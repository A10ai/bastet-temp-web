'use client';import { useState } from 'react';import { useTranslations } from 'next-intl';import Link from 'next/link';import { Instagram, Facebook, Music2, Youtube, Mail, ArrowRight } from 'lucide-react';import { cn } from '@/lib/utils';const Footer = () => {  const tFooter = useTranslations('footer');  const tNav = useTranslations('nav');  const [email, setEmail] = useState('');  const [subscribeStatus, setSubscribeStatus] = useState<'idle' | 'loading' | 'success' | 'error'>('idle');  const handleSubscribe = (e: React.FormEvent) => {    e.preventDefault();    setSubscribeStatus('loading');    // Simulate API call    setTimeout(() => {      if (email) {        setSubscribeStatus('success');        setEmail('');        setTimeout(() => setSubscribeStatus('idle'), 3000);      } else {        setSubscribeStatus('error');      }    }, 500);  };  const exploreLinks = [    { href: '#apartments', label: tNav('apartments') },    { href: '#facilities', label: tNav('facilities') },    { href: '#smart-hotel', label: tNav('smartHotel') },    { href: '#location', label: tNav('location') },    { href: '#digital-nomads', label: tNav('digitalNomads') },
